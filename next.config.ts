import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['www.creativefabrica.com', 'cdn.i-scmp.com', 'raw.githubusercontent.com', 'cdn.britannica.com', 'alchetron.com', 'upload.wikimedia.org', 'i2-prod.walesonline.co.uk'],
  },
};

export default nextConfig;
